# lister-elm-rest-routing

Building off of [lister-elm](https://github.com/jackrzhang/lister-elm), adding 'SPA' navigation and a JSON REST API.

#### Build Instructions
Run `npm run prod`. Then open `localhost:3000` in your browser.
